<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="KvizAlkalmazas.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Matematika Kvíz" Height="502" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
		Background="#E0E2DB">

    <Window.Resources>

        <SolidColorBrush x:Key="primaryC" Color="#E6AF2E"/>
        <SolidColorBrush x:Key="secondaryC" Color="#191716"/>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource primaryC}"/>
        </Style>

        <Style x:Key="QuestionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource primaryC}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="RadioButtonStyle" TargetType="RadioButton">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="20,5,0,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
            <Setter Property="Padding" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource secondaryC}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="{StaticResource primaryC}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="10" Height="45">
                                <ContentPresenter 
                                    HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"/>    
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <Style x:Key="ListboxStyle" TargetType="ListBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource secondaryC}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border 
                            MinWidth="16"
                            Width="Auto"
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="10">
                            <ItemsPresenter
                                    HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        
        
<Style TargetType="ListBoxItem">
            <Setter Property="Width" Value="auto"></Setter>
    <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
            <Setter Property="Foreground" Value="{StaticResource primaryC}"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Trigger>
    </Style.Triggers>
</Style>
        
    </Window.Resources>


    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Üdvözlő Panel -->
        <StackPanel x:Name="WelcomePanel" Grid.Row="0" Grid.RowSpan="5" Visibility="Visible">
            <TextBlock Style="{StaticResource HeaderStyle}" HorizontalAlignment="Center" Text="Kvíz Alkalmazás"/>

            <TextBlock FontSize="14" TextWrapping="Wrap" Margin="0,0,0,20" TextAlignment="Center"><Run Text="Üdvözöljük a Kvíz alkalmazásban!"/><LineBreak/><LineBreak/><Run Text="• 10 véletlenszerű kérdést fogsz kapni 50 kérdésből"/><LineBreak/><Run Text="• Minden kérdéshez 4 válaszlehetőség tartozik"/><LineBreak/><Run Text="• Válassz egy válaszlehetőséget és kattints a &quot;Válasz rögzítése&quot; gombra"/><LineBreak/><Run Text="• Az eredmény automatikusan mentésre kerül"/></TextBlock>
            <TextBlock FontSize="16" TextWrapping="Wrap" FontWeight="Bold" Margin="0,0,0,10" TextAlignment="Center">
A Kérdések témaköre az alábbi listában választható ki:           
            </TextBlock>
            <ListBox x:Name="CategoriesListBox" Style="{StaticResource ListboxStyle}"   HorizontalAlignment="Center"
         Width="Auto" MinWidth="120" Margin="0,0,0,19"  >

            </ListBox>




            <Button Content="Kvíz Indítása" Style="{StaticResource ButtonStyle}"
                    Width="200"
                    Click="StartQuiz_Click"/>
        </StackPanel>


        <!-- Kvíz Panel -->
        <StackPanel x:Name="QuizPanel" Grid.Row="0" Grid.RowSpan="5" Visibility="Collapsed">
            <!-- Fejléc -->
            <Grid Margin="0,0,0,20">
                <TextBlock Text="Matematika Kvíz" Style="{StaticResource HeaderStyle}"
                           HorizontalAlignment="Left"/>
                <TextBlock x:Name="QuestionCounter" Text="Kérdés: 1/10" FontSize="16"
                           VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>

            <!-- Haladás jelző -->
            <ProgressBar x:Name="QuestionProgress" Height="10" Margin="0,0,0,20"
                         Minimum="0" Maximum="10" Value="5"
                         Foreground="{StaticResource secondaryC}"/>

            <!-- Kérdés -->
            <Border Background="#ECF0F1" CornerRadius="5" Padding="20" Margin="0,0,0,20">
                <TextBlock x:Name="QuestionText" Style="{StaticResource QuestionStyle}" Text="asdasdasdasdasdasdasdaasdasdasdasdasdasdasda asdasdasdasdasdasdasda asdasdasdasdasdasdasda  sdasdasdasdasdasdasdas"/>
            </Border>

            <!-- Válaszlehetőségek -->
            <StackPanel x:Name="AnswersPanel">
                <RadioButton x:Name="Answer1" Content="Válasz 1" GroupName="Answers"
                             Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton x:Name="Answer2" Content="Válasz 2" GroupName="Answers"
                             Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton x:Name="Answer3" Content="Válasz 3" GroupName="Answers"
                             Style="{StaticResource RadioButtonStyle}"/>
                <RadioButton x:Name="Answer4" Content="Válasz 4" GroupName="Answers"
                             Style="{StaticResource RadioButtonStyle}"/>
            </StackPanel>

            <!-- Gomb -->
            <Button x:Name="SubmitAnswerButton" Content="Válasz rögzítése"
                    Style="{StaticResource ButtonStyle}"
                    Width="200" Margin="0,30,0,0"
                    HorizontalAlignment="Center"
                    Click="SubmitAnswer_Click"/>
        </StackPanel>

        <!-- Eredmény Panel -->
        <StackPanel x:Name="ResultPanel" Grid.Row="0" Grid.RowSpan="5"
                    Visibility="Collapsed" HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Style="{StaticResource HeaderStyle}" HorizontalAlignment="Center">
                Kvíz Befejezve!
            </TextBlock>

            <TextBlock x:Name="ResultText" FontSize="20" TextAlignment="Center"
                       Margin="0,20,0,20" TextWrapping="Wrap"/>

            <Button Content="Új Kvíz Indítása" Style="{StaticResource ButtonStyle}"
                    Width="200" Click="RestartQuiz_Click"/>
        </StackPanel>
    </Grid>
</Window>